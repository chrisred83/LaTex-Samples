La carpeta imgs1 funciona como un contenedor de imágenes.
La carpeta es tomada como una ubicación externa en donde se puede encontrar un elemento multimedia que va a ser incluido en el documento de LaTex.
Si el nombre de la carpeta cambia, también debe cambiar en la plantilla, si no se actualiza el nombre mostrará un error al compilar el documento.
